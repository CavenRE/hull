// Package manifest defines the hull.yaml project schema (sites and
// multi-container apps): parsing, validation, defaults, and
// schema-version migration. The manifest is the source of truth for a
// project; everything else is generated from it. (Phase 1)
package manifest
