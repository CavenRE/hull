# Generated by Hull from hull.yaml , do not edit directly.
# Regenerate with: hull render
name: oldblog
services:
  app:
    image: wordpress:6.4
    entrypoint:
      - bash
      - -c
      - chmod -R a+rX /var/www/html 2>/dev/null || true; exec docker-entrypoint.sh "$@"
      - --
    command: apache2-foreground
    environment:
      - WORDPRESS_CONFIG_EXTRA=define('DISABLE_WP_CRON', true);
      - WORDPRESS_DB_HOST=db
      - WORDPRESS_DB_NAME=oldblog
      - WORDPRESS_DB_PASSWORD=
      - WORDPRESS_DB_USER=root
      - WP_ENVIRONMENT_TYPE=local
    volumes:
      - ./:/var/www/html
      - /home/test/.hull/system/php/opcache.ini:/usr/local/etc/php/conf.d/zz-hull-opcache.ini:ro
    ports:
      - 127.0.0.1::80
    extra_hosts:
      - host.docker.internal:host-gateway
    labels:
      - com.hull.managed=true
      - caddy=oldblog.test
      - caddy.reverse_proxy={{upstreams 80}}
      - caddy.tls=internal
    networks:
      - default
      - caddy
  db:
    image: mysql:8.0
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes
      - MYSQL_DATABASE=oldblog
    volumes:
      - db_data:/var/lib/mysql
    labels:
      - com.hull.managed=true
    networks:
      - default
      - caddy
networks:
  caddy:
    external: true
volumes:
  db_data: null
