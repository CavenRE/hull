# Generated by Hull from hull.yaml , do not edit directly.
# Regenerate with: hull render
name: legacy-app
services:
  app:
    image: serversideup/php:8.1-fpm-nginx
    user: "0:0"
    volumes:
      - ./:/var/www/html
      - /home/test/.hull/system/php/opcache.ini:/usr/local/etc/php/conf.d/zz-hull-opcache.ini:ro
      - /home/test/.hull/system/php/hull-composer-install.sh:/etc/entrypoint.d/15-hull-composer-install.sh:ro
      - vendor:/var/www/html/vendor
    ports:
      - 127.0.0.1::8080
    extra_hosts:
      - host.docker.internal:host-gateway
    labels:
      - com.hull.managed=true
      - caddy=legacy-app.test
      - caddy.reverse_proxy={{upstreams 8080}}
      - caddy.tls=internal
    networks:
      - default
      - caddy
    depends_on:
      db:
        condition: service_healthy
  db:
    image: mariadb:lts
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes
      - MYSQL_DATABASE=legacy_app
    volumes:
      - db_data:/var/lib/mysql
    labels:
      - com.hull.managed=true
    networks:
      - default
      - caddy
    healthcheck:
      test:
        - CMD-SHELL
        - healthcheck.sh --connect --innodb_initialized
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
networks:
  caddy:
    external: true
volumes:
  db_data: null
  vendor: null
