# Generated by Hull from hull.yaml — do not edit directly.
# Regenerate with: hull render
name: blog
services:
  app:
    image: wordpress:latest
    environment:
      - WORDPRESS_DB_HOST=db
      - WORDPRESS_DB_NAME=blog
      - WORDPRESS_DB_PASSWORD=
      - WORDPRESS_DB_USER=root
    volumes:
      - ./:/var/www/html
      - /home/test/.hull/system/php/xdebug.ini:/usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini:ro
    extra_hosts:
      - host.docker.internal:host-gateway
    labels:
      - caddy=blog.test
      - caddy.reverse_proxy={{upstreams 80}}
      - caddy.tls=internal
    networks:
      - default
      - caddy
  db:
    image: mariadb:lts
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes
      - MYSQL_DATABASE=blog
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - default
      - caddy
networks:
  caddy:
    external: true
volumes:
  db_data: null
