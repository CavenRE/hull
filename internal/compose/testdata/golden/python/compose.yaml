# Generated by Hull from hull.yaml , do not edit directly.
# Regenerate with: hull render
name: pyapp
services:
  app:
    image: python:3.13-slim
    command: sh -c '[ -d /opt/venv/bin ] || python -m venv /opt/venv; if [ -f requirements.txt ]; then h=$$(md5sum requirements.txt | cut -c1-32); if [ "$$(cat /opt/venv/.hull-installed 2>/dev/null)" != "$$h" ]; then pip install -q -r requirements.txt && echo "$$h" > /opt/venv/.hull-installed; fi; fi; exec python app.py'
    working_dir: /app
    environment:
      - PATH=/opt/venv/bin:/usr/local/bin:/usr/local/sbin:/usr/bin:/usr/sbin:/bin:/sbin
      - PORT=8000
      - PYTHONUNBUFFERED=1
      - VIRTUAL_ENV=/opt/venv
    volumes:
      - ./:/app
      - venv:/opt/venv
      - pip_cache:/root/.cache/pip
    ports:
      - 127.0.0.1::8000
    extra_hosts:
      - host.docker.internal:host-gateway
    labels:
      - com.hull.managed=true
      - caddy=pyapp.test
      - caddy.reverse_proxy={{upstreams 8000}}
      - caddy.tls=internal
    networks:
      - default
      - caddy
networks:
  caddy:
    external: true
volumes:
  pip_cache: null
  venv: null
