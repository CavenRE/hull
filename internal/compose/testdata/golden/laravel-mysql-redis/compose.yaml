# Generated by Hull from hull.yaml — do not edit directly.
# Regenerate with: hull render
name: shop
services:
  app:
    image: serversideup/php:8.3-fpm-nginx
    volumes:
      - ./:/var/www/html
      - /home/test/.hull/system/php/xdebug.ini:/usr/local/etc/php/conf.d/99-xdebug.ini:ro
    extra_hosts:
      - host.docker.internal:host-gateway
    labels:
      - caddy=shop.test
      - caddy.reverse_proxy={{upstreams 8080}}
      - caddy.tls=internal
    networks:
      - default
      - caddy
  db:
    image: mysql:8.0
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes
      - MYSQL_DATABASE=shop
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - default
      - caddy
  redis:
    image: redis:alpine
    volumes:
      - redis_data:/data
    networks:
      - default
networks:
  caddy:
    external: true
volumes:
  db_data: null
  redis_data: null
