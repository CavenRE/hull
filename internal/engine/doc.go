// Package engine is Hull's orchestration core: it owns project lifecycle
// (create, start, stop, destroy), coordinates the manifest, compose,
// dockerx, router, and services packages, and exposes the operations the
// API layer serves. (Phase 2)
package engine
