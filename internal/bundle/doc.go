// Package bundle implements hull-bundle.zip export and import: a
// versioned manifest, project files with configurable excludes, fresh
// database dumps from linked services, and optional secret-stripped env
// files. (Phase 6)
package bundle
