// Package certs manages Hull's local root CA and its installation into
// OS and browser trust stores (system store, NSS/Firefox) on Windows,
// macOS, and Linux , mkcert-style. (Phase 4)
package certs
