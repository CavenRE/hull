// Package templates embeds Hull's built-in project templates (laravel,
// wordpress, plain) and service mix-ins (postgres, mysql, mariadb,
// redis), plus their scaffold hooks, ported from v1. User-defined
// template directories layer on top. (Phase 1)
package templates
