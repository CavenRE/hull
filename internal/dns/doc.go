// Package dns implements the embedded wildcard resolver answering
// *.<tld> with 127.0.0.1, registered with the host OS via the platform
// package. Replaces v1's dnsmasq/NetworkManager configuration. (Phase 4)
package dns
